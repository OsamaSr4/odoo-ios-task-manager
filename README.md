# odoo-ios-task-manager
SwiftUI-based iOS application using MVVM architecture that integrates with Odoo ERP via JSON-RPC to authenticate users, fetch assigned tasks, create new tasks, and update task statuses.
